# Deployment Environment Variables
ENVIRONMENT=development

# Database Configuration
# NOTE: This uses local SQLite for the assignment scope. In production, provide a Postgres URL.
DATABASE_URL=sqlite:///./duolingo.db

# Authentication / Gamification defaults
DEFAULT_USER_ID=1
DEFAULT_COURSE_ID=1

# Security
CORS_ORIGINS=["http://localhost:3000"]
